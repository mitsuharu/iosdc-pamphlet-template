FROM node:24-alpine

WORKDIR /workspace

# package.json の packageManager で指定した Yarn を使う。
# イメージに同梱されている Yarn 1.x のままだと、Berry 形式の
# yarn.lock を読めずにインストールが失敗する。
RUN corepack enable

COPY package.json yarn.lock .yarnrc.yml ./
RUN yarn install --immutable && yarn cache clean
